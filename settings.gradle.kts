pluginManagement {
    repositories {
        // ✅ Keep Google’s Maven repository open for all Firebase libraries
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Skillshare"
include(":app")
